function oddCount(n){
 let newArr = []
 for(let i = 1; i < n; i++){
   if(i % 2 !== 0){ newArr.push(i)}
 }
  return newArr.length
}

//this loop does not pass because it takes too long.

function oddCount(n){
  return Math.floor(n/2)
}
