function checkExam(a1, a2) {
 let count = 0
 for(let i=0; i < a1.length; i++){
   if(a1[i] === a2[i]) {count += 4}
  else if(a2[i] == ''){count += 0}
  else{count -= 1}
  }
return count < 1? 0 : count
}
