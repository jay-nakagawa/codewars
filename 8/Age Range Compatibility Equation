function datingRange(age){
 if(age<=14){
   return (Math.floor(age - 0.10 * age)) + "-" + Math.floor((age + (0.10 * age)))
 }
  
  
 let min = Math.floor(age/2 + 7)
 let max = Math.ceil((age-7)*2)
 return min + "-" + max
}
