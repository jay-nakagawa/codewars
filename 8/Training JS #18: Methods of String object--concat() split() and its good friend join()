function splitAndMerge(string, separator) {
  let str = string.split(' ')
  let result = ""
  
  for(let i = 0; i < str.length; i++){
    result +=  str[i].split('').join(separator) + " "
  }
  return result.trim()
}
