
function barTriang(point1, point2, point3) {
  let sumX = point1[0] + point2[0] + point3[0];
  let sumY = point1[1] + point2[1] + point3[1];

  const n = 3;
  const barycenterX = (sumX / n).toFixed(4);
  
  const barycenterY = (sumY / n).toFixed(4)

  return [+barycenterX, +barycenterY];
}
