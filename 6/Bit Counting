var countBits = function(n) {
  return n.toString(2).split('').filter(x=> x == 1).length
};


/////

var countBits = function(n) {

  let toBin = n.toString(2).split('')

 return toBin.reduce((a,b)=>a+Number(b),0)
};
