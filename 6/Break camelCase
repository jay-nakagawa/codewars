// complete the function
function solution(string) {
  let newstr = ''  
  for(let i=0; i<string.length;i++){
    if(string[i]===string[i].toUpperCase()){newstr+= ` ${string[i]}`}
    else{newstr += string[i]}
    }
  return newstr
}
